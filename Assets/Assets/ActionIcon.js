
var Ball : GameObject;
var DangerIcon : Sprite;
var DodgeUpIcon : Sprite;
var DodgeDownIcon : Sprite;
var DodgeLeftIcon : Sprite;
var DodgeRightIcon : Sprite; 

function Start () {
	

}

function Update () {


}

function UpdateIcon ( player : GameObject) {

	/*
	var distanceToBall : float = Vector3.Distance(player.transform.position,Ball.transform.position);
	if (distanceToBall <=2 ) {
		if ( !gameObject.GetComponent(UI.Image).enabled ) 
			gameObject.GetComponent(UI.Image).enabled = true;
		gameObject.GetComponent(UI.Image).sprite = DodgeUpIcon;
	} else if (distanceToBall <= 6) {
		if ( !gameObject.GetComponent(UI.Image).enabled ) 
			gameObject.GetComponent(UI.Image).enabled = true;
		gameObject.GetComponent(UI.Image).sprite = DangerIcon;
	} else {
		gameObject.GetComponent(UI.Image).enabled = false;
	}
	*/
	

} 



